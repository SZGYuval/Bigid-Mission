apiVersion: v1
kind: Service
metadata:
  name: web-app-service
  namespace: web-app
spec:
  selector:
    app: web
  type: ClusterIP
  ports:
    - targetPort: 5000
      port: 5000
      protocol: TCP